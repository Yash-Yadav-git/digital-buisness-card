import React from "react";
import ReactDOM from "react-dom";
import Header from "./Components/Header";

const App = () => {
  ReactDOM.render(
    <React.StrictMode>
      <Header></Header>
    </React.StrictMode>
  );
};

export default App;
